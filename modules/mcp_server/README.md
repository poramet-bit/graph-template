# mcp_server

MCP server module — bridges the source system (e.g. E-budget) and pulls raw data
into `data_layer`. Read-only sync on a schedule; does not touch the source system's
write path.

Status: not yet implemented. Integration point reserved for when MCP connection
details are finalized.
