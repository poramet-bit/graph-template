# data_layer

Structured storage for data synced by `mcp_server`. Target: DuckDB or SQLite
(embedded, no separate server process). Optional vector DB (ChromaDB / Qdrant)
for free-text fields, added later if needed.

Status: not yet implemented.
